Feature: this is for api tests

  Background:

  Scenario: testing rest api testing
    Given url 'https://api.spacexdata.com/v4/launches/latest'
    * method GET
    * print response
    * print responseTime
    * print responseType
    * match responseType == 'json'
    * print responseStatus == 200
#    * match response == '#json'
    * match response.launchpad == '#string'
    * match response.payloads == '#[]'
    * match response.payloads[0] == '#string'
    * match response.rocket == '#string'
    * match response.crew == '#[]'
    * match response.crew[0] == '#string'
    * match response.crew[1] == '#string'
    * match response.crew[2] == '#string'
    * match response.crew[3] == '#string'
    * match response.date_unix == '#number'
    * match response.cores == '#[]'
    * match response.cores[0].core == '#string'
    * match response.cores[0].flight == '#number'
    * match response.cores[0].landing_type == '#string'
    * match response.cores[0].gridfins == '#boolean'
    * match response.cores[0].landing_attempt == '#boolean'
    * match response.cores[0].legs == '#boolean'
    * match response.cores[0].landpad == '#string'
    * match response.cores[0].reused == '#boolean'
    * match response.cores[0].landing_success == '#boolean'
    * match response.auto_update == '#boolean'
    * match response.date_precision == '#string'
    * match response.links.patch.small == '#string'
    * match response.links.patch.large == '#string'
    * match response.links.webcast == '#string'
    * match response.links.flickr.small == '#[]'
    * match response.links.flickr.original == '#[]'
    * match response.links.reddit.campaign == '#string'
    * match response.links.reddit.launch == '#string'
    * match response.links.reddit.media == '#null'
    * match response.links.reddit.recovery == '#null'
    * match response.links.wikipedia == '#string'
    * match response.links.youtube_id == '#string'
    * match response.links.presskit == '#null'
    * match response.links.article == '#null'
    * match response.details == '#string'
    * match response.id == '#string'
    * match response.net == '#boolean'
    * match response.capsules == '#[]'
    * match response.capsules[0] == '#string'
    * match response.static_fire_date_utc == '#string'
    * match response.failures == '#[]'
    * match response.date_local == '#string'
    * match response.flight_number == '#number'
    * match response.launch_library_id == '#string'
    * match response.fairings == '#null'
    * match response.ships == '#[]'
    * match response.ships[0] == '#string'
    * match response.ships[1] == '#string'
    * match response.ships[2] == '#string'
    * match response.date_utc == '#string'
    * match response.static_fire_date_unix == '#number'
    * match response.success == '#boolean'
    * match response.tbd == '#boolean'
    * match response.name == '#string'
    * match response.window == '#number'
    * match response.upcoming == '#boolean'
    * match response.launchpad == '#string'
    * match response.payloads == '#[]'
    * match response.payloads[0] == '#string'
    * match response.rocket == '#string'
    * match response.crew == '#[]'
    * match response.crew[0] == '#string'
    * match response.crew[1] == '#string'
    * match response.crew[2] == '#string'
    * match response.crew[3] == '#string'
    * match response.date_unix == '#number'
    * match response.cores == '#[]'
    * match response.cores[0].core == '#string'
    * match response.cores[0].flight == '#number'
    * match response.cores[0].landing_type == '#string'
    * match response.cores[0].gridfins == '#boolean'
    * match response.cores[0].landing_attempt == '#boolean'
    * match response.cores[0].legs == '#boolean'
    * match response.cores[0].landpad == '#string'
    * match response.cores[0].reused == '#boolean'
    * match response.cores[0].landing_success == '#boolean'
    * match response.auto_update == '#boolean'
    * match response.date_precision == '#string'
    * match response.links.patch.small == '#string'
    * match response.links.patch.large == '#string'
    * match response.links.webcast == '#string'
    * match response.links.flickr.small == '#[]'
    * match response.links.flickr.original == '#[]'
    * match response.links.reddit.campaign == '#string'
    * match response.links.reddit.launch == '#string'
    * match response.links.reddit.media == '#null'
    * match response.links.reddit.recovery == '#null'
    * match response.links.wikipedia == '#string'
    * match response.links.youtube_id == '#string'
    * match response.links.presskit == '#null'
    * match response.links.article == '#null'
    * match response.details == '#string'
    * match response.id == '#string'
    * match response.net == '#boolean'
    * match response.capsules == '#[]'
    * match response.capsules[0] == '#string'
    * match response.static_fire_date_utc == '#string'
    * match response.failures == '#[]'
    * match response.date_local == '#string'
    * match response.flight_number == '#number'
    * match response.launch_library_id == '#string'
    * match response.fairings == '#null'
    * match response.ships == '#[]'
    * match response.ships[0] == '#string'
    * match response.ships[1] == '#string'
    * match response.ships[2] == '#string'
    * match response.date_utc == '#string'
    * match response.static_fire_date_unix == '#number'
    * match response.success == '#boolean'
    * match response.tbd == '#boolean'
    * match response.name == '#string'
    * match response.window == '#number'
    * match response.upcoming == '#boolean'

